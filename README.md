
# 🚀 KidsJoy Learning Adventure - راهنمای نصب و اجرا

این یک اپلیکیشن آموزشی هوشمند برای کودکان است که با استفاده از هوش مصنوعی Gemini طراحی شده است.

## 🔑 تنظیم کلید API در Cloudflare Pages (بسیار مهم)
اگر از **Cloudflare Pages** استفاده می‌کنید، برای اینکه دکمه‌های جادویی کار کنند، باید کلید خود را به این صورت ست کنید:

۱. وارد پنل **Cloudflare Dashboard** شوید.
۲. به بخش **Workers & Pages** بروید و پروژه خود را انتخاب کنید.
۳. به تب **Settings** و سپس بخش **Environment Variables** بروید.
۴. در قسمت **Production**، روی دکمه **Add variable** کلیک کنید:
   - Variable Name: `API_KEY`
   - Value: (کلید API خود را که از Google AI Studio گرفتید اینجا وارد کنید)
۵. **مهم:** بعد از ذخیره، باید یک بار دیگر پروژه را **Redeploy** کنید تا تغییرات اعمال شود.

## 🔑 تنظیم در Vercel یا Netlify
۱. وارد تنظیمات پروژه شوید.
۲. به بخش **Environment Variables** بروید.
۳. متغیر `API_KEY` را با مقدار کلید خود اضافه کنید.

## ✨ قابلیت‌ها
- ۲۵ دسته‌بندی آموزشی با بیش از ۱۰۰ کلمه پایه.
- ۶ بازی تعاملی (کارت‌های حافظه، کوییز، هجی کردن و...).
- تبدیل متن به صدا (TTS) با هوش مصنوعی برای لهجه نیتیو.
- تولید تصاویر هنری اختصاصی برای هر کلمه با استفاده از Gemini.
- قابلیت نصب روی موبایل (Add to Home Screen) به عنوان PWA.

## 🔑 دریافت کلید API
اگر هنوز کلید ندارید، به [Google AI Studio](https://aistudio.google.com/) بروید و روی **Create API Key** کلیک کنید.
